import React from "react";
import {
  StyleSheet,
  Text,
  View,
  Button,
  Image,
  ScrollView,
  TouchableOpacity,
} from "react-native";

const IndexScreen = () => {
  return <Text>IndexScreen</Text>;
};

const styles = StyleSheet.create({});

export default IndexScreen;
